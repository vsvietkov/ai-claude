---
name: example-skill
description: Example skill — replace this description with a specific, trigger-rich summary of WHEN Claude should invoke this skill (keywords, task shapes, file types). This description is the only thing Claude sees when deciding whether to load the skill.
---

# Example skill

This is a template Agent Skill shipped by `personal-toolkit`. A skill is a
folder with a `SKILL.md` whose frontmatter `description` tells Claude when to
load it; the body below is the full instruction set loaded on demand.

## When to use

Explain the concrete situations this skill applies to.

## Instructions

1. Step one.
2. Step two.
3. Step three.

## Supporting files (optional)

Place reference docs, scripts, or templates alongside this file and link to
them with relative paths, e.g. `references/guide.md` or `scripts/run.sh`.
Claude loads them only when needed, keeping context lean.
