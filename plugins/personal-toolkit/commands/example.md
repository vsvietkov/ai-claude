---
description: Example slash command — replace with your own reusable command.
argument-hint: [optional-arg]
---

# Example command

This is a template slash command shipped by `personal-toolkit`. Invoke it with
`/example` (or `/personal-toolkit:example` if the short name collides).

Replace the body below with the instructions Claude should follow when this
command runs. Anything you type after the command is available as `$ARGUMENTS`.

## Task

User input: $ARGUMENTS

1. Describe the first step here.
2. Describe the second step here.
3. Report the result concisely.
